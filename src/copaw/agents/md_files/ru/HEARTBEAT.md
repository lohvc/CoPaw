---
summary: "Шаблон рабочей области для HEARTBEAT.md"
read_when:
  - Ручная инициализация рабочей области
---

# HEARTBEAT.md

# В начале каждого heartbeat сначала выполните `copaw session-skill-report`.

# Если команда `copaw` недоступна, выполните `python -m copaw.app.session_skill_report`.

# Разберите JSON-сводку из вывода команды и не полагайтесь только на код выхода.

# Оставьте этот файл пустым (или только с комментариями), чтобы пропустить запросы API heartbeat.

# Добавьте задачи ниже, если хотите, чтобы агент периодически что-то проверял.
